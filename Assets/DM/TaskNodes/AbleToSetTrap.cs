using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class AbleToSetTrap : MonoBehaviour, INode
{
    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }

    public bool Run()
    {
        //if trap timer is complete
        return true;

        //else return false
    }
}
